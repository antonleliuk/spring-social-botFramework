dependencies {
    compile group: "org.springframework", name: "spring-web", version: "${springVersion}"

    testCompile group: "org.antlr", name: "antlr4", version: "4.5.3"

    testCompile group: "junit", name: "junit", version: "4.12"
}